# M0：可信测试基线修复报告

## 1. 本次工作的目的

本次工作处理重构计划 M0 中最直接影响可信度的三类问题：

1. 修正评估 loss 随 batch size 改变的计算错误；
2. 移除默认测试对本机 checkpoint 和 distribution 目录的依赖；
3. 移除一个只验证 round 索引、却会初始化完整 FL runner 并隐式下载 MNIST 的测试依赖。

M0 的定位不是重构系统架构，而是建立后续迁移可以信任的测试基线。如果迁移前的指标和测试本身不稳定，就无法判断新旧模块之间的差异来自重构错误、随机状态，还是原实现缺陷。

## 2. 工作位置与当前状态

- Git 分支：`codex/m0-baseline`
- 隔离 worktree：`/private/tmp/meta-stackelberg-m0-baseline`
- 基础分支：`feat/attacker`
- 基础提交：`3e6562c feat: add mixed backdoor Meta-SG tooling`
- 当前状态：修改尚未合并回 `feat/attacker`

原工作目录中的未跟踪文件 `meta_sg_training_path.html` 已按用户要求删除。该文件是生成的可视化产物，不参与代码、测试或训练。

## 3. 修复前的基线

在不复用原项目 `data/` 和 `outputs/` 的隔离 worktree 中执行：

```bash
/Users/antik/anaconda3/bin/python3 -m pytest -q meta_sg/tests tests
```

结果为：

```text
317 passed, 3 failed
```

三个失败分别是：

1. 默认 checkpoint 解析测试要求存在被 Git 忽略的 `rl_policy_latest.pt`；
2. distribution 目录解析测试会读取同一个本机 checkpoint；
3. `Paper3DSandboxCoordinator` 起始 round 测试构造完整 runner，进而尝试联网下载 MNIST。

原工作目录之前表现为 `318 passed, 2 failed`，是因为原目录已有本地 `data/MNIST`。隔离 worktree 暴露了第三个被本地数据缓存掩盖的问题。

## 4. Loss 计算错误及修复

### 4.1 原实现的问题

文件：`fl_sandbox/evaluation/evaluator.py`

原实现使用：

```python
loss += criterion(outputs, labels).item()
```

`torch.nn.CrossEntropyLoss()` 默认返回当前 batch 的平均 loss。直接累加各 batch 的平均值，得到的是“batch 平均值之和”，而不是整个数据集的样本平均 loss。

对于同一模型和同样 8 个样本，修复前的结果为：

```text
batch_size=1  loss=5.545177
batch_size=2  loss=2.772589
batch_size=4  loss=1.386294
batch_size=8  loss=0.693147
```

模型、样本和预测均未改变，loss 却随 batch 数量成比例变化。这会影响：

- Meta-SG 的 loss-based defender reward；
- TD3/SAC 的奖励尺度；
- 固定防御和学习防御的比较；
- query gate 和 adaptation 选择；
- TensorBoard loss 曲线；
- 使用不同 `eval_batch_size` 的实验可比性。

### 4.2 新增回归测试

新增文件：

```text
tests/meta_stackelberg/unit/test_evaluation_loss.py
```

测试使用相同的 8 个样本和固定二分类模型，分别用 batch size 1、2、4、8 评估，并要求所有情况都返回：

```text
loss = log(2) ≈ 0.693147
accuracy = 1.0
```

TDD 红灯阶段结果：

```text
3 failed, 1 passed
```

batch size 1、2、4 均按预期失败，证明测试能够捕获原始问题；batch size 8 因为只有一个 batch 而碰巧通过。

### 4.3 修复方式

新实现按样本数累加，再除以总样本数：

```python
batch_size = labels.size(0)
total_loss += criterion(outputs, labels).item() * batch_size
total += batch_size
mean_loss = total_loss / total if total else 0.0
```

该修复没有改变：

- 模型推理；
- accuracy 计算；
- clean/backdoor DataLoader；
- optimizer 或训练逻辑；
- reward 公式本身。

它只把评估 loss 的定义修正为标准的样本平均交叉熵。

修复后的聚焦测试结果：

```text
4 passed
```

## 5. 移除本机 Checkpoint 依赖

修改文件：

```text
meta_sg/tests/test_stackelberg_defender_td3.py
```

### 5.1 默认 checkpoint 解析测试

原测试直接要求仓库中存在：

```text
fl_sandbox/outputs/.../checkpoints/rl_policy_latest.pt
```

但是 `fl_sandbox/outputs/` 被 `.gitignore` 忽略，因此干净克隆无法满足该条件。

新测试使用 pytest 的 `tmp_path` 创建临时 checkpoint，并通过 `monkeypatch` 临时替换默认 checkpoint 常量。这样仍然验证：

- 空 CLI 路径会选择默认 checkpoint；
- 路径解析返回正确文件；
- 文件存在性检查仍然生效。

生产代码中的默认 checkpoint 行为没有在本次修复中改变。

### 5.2 Distribution 目录解析测试

新测试在 `tmp_path` 下：

1. 创建临时 distribution 目录；
2. 创建包含 `config.rl_distribution_dir` 的临时 Torch checkpoint；
3. 调用真实 `resolve_distribution_dir()`；
4. 断言解析结果等于临时目录。

该测试仍然覆盖真实 checkpoint 读取和配置解析，但不再依赖历史训练产物。

## 6. 移除测试中的隐式 MNIST 下载

修改文件：

```text
meta_sg/tests/test_stackelberg_paper3d_sandbox.py
```

原测试只想验证：

```python
start_round_idx=101 -> _initial_round_idx() == 100
```

但它调用了 `Paper3DSandboxCoordinator(...)` 的完整构造函数，间接创建 `MinimalFLRunner`、加载数据集，并在隔离环境中尝试联网下载 MNIST。

新测试通过 `object.__new__()` 构造未初始化对象，只设置该纯函数真正需要的 `config`：

```python
coordinator = object.__new__(Paper3DSandboxCoordinator)
coordinator.config = SandboxConfig(start_round_idx=101)
```

这样测试仍然验证 round 索引逻辑，但不会初始化模型、数据集、客户端或网络下载。

## 7. 修改文件汇总

```text
M  fl_sandbox/evaluation/evaluator.py
M  meta_sg/tests/test_stackelberg_defender_td3.py
M  meta_sg/tests/test_stackelberg_paper3d_sandbox.py
A  tests/meta_stackelberg/unit/test_evaluation_loss.py
A  M0_可信测试基线修复报告.md
```

各文件职责：

| 文件 | 修改内容 |
|---|---|
| `fl_sandbox/evaluation/evaluator.py` | 将 clean loss 修正为样本平均 loss |
| `tests/meta_stackelberg/unit/test_evaluation_loss.py` | 防止 batch-size 依赖问题回归 |
| `meta_sg/tests/test_stackelberg_defender_td3.py` | 使用临时 checkpoint 和 distribution 目录 |
| `meta_sg/tests/test_stackelberg_paper3d_sandbox.py` | 避免无关的 MNIST 初始化和下载 |
| `M0_可信测试基线修复报告.md` | 记录修复原因、范围和验证证据 |

## 8. 最终验证结果

验证环境：

```text
Python 3.12.7
NumPy 1.26.4
pytest 7.4.4
PyTorch 2.12.0
```

完整验证命令：

```bash
/Users/antik/anaconda3/bin/python3 -m pytest -q \
  meta_sg/tests tests tests/meta_stackelberg
```

2026-07-11 的最新执行结果：

```text
324 passed in 10.94s
```

另外执行了：

```bash
git diff --check
```

命令返回成功，没有发现空白符错误。

## 9. 本次没有做的工作

为了保持 M0 修复范围可控，本次没有：

- 创建 `meta_stackelberg/` 新包；
- 搬运 FL、攻击、防御或 RL 模块；
- 修改 TD3、SAC、MATD3 数学；
- 修改 reward 公式；
- 修改生产环境的默认 checkpoint 选择策略；
- 修复仓库 `.venv` 的失效解释器链接；
- 合并、推送或创建 Pull Request。

## 10. 已发现但尚未处理的环境问题

仓库文档要求使用 `.venv/bin/python`，但当前 `.venv` 指向缺少标准库的旧 Python 安装，无法启动。隔离 worktree 中未继承原目录的 shell 路径解析，因此本次验证固定使用：

```text
/Users/antik/anaconda3/bin/python3
```

环境重建、依赖声明和可复现安装应作为 M0 后续治理项或 M1 开始前的独立任务处理，不能用修改测试路径来掩盖。

## 11. 后续建议

1. 先将本分支的 M0 修复合并回 `feat/attacker`；
2. 合并后在原工作目录再次运行完整测试；
3. 记录历史实验的 loss 定义，避免把旧的 batch-sum loss 与新的 sample-mean loss 直接比较；
4. 再开始 M1：建立 `meta_stackelberg/` 骨架、核心数据类型、随机状态和配置边界。
